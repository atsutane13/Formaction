{% for article in articles %}
<article>
    <h2>{{ article.title }}</h2>
    <p>publié par 
        <a href="{{ path('voirUser', {id: article.idUser}) }}">
            {{ article.usersId.username }}
        </a>
        le {{article.datePubli}}
    </p>
    <p><a href="{{ path('voirArticle', {id: article.idArticle}) }}">Voir</a></p>
</article>
{% endfor %}