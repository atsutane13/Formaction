{% extends "layout.html.twig" %}
	
	{% block title %}5 derniers article{% endblock %}
	
	{% block content %}
			<h2>Mes Formations</h2>
			{% for formation in formations %}
			<article>
				<h2>{{ formation.title }}</h2>
				<p><a href="{{ path('voirFormation', {id: formation.id}) }}">Voir</a></p>

			</article>
			{% endfor %}
		{% endblock %}