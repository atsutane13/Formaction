	{% extends "layout.html.twig" %}
	
	{% block title %}Un utilisateur{% endblock %}
	
	{% block content %}
            <h2>utilisateur:</h2>
			<div class="row">
					<div class="col-md-8">
						<article>
				<h2>Formation proposée par : {{ intervenant.nom }}</h2>
				<p>Lien de contact : {{ intervenant.contact }}</p>
				<p>{{ intervenant.phone }}</p>
			</article>
			
			
			</div>


			{% for article in articles %}
			<article>
				<div>
				<h4>Formation : {{ article.title }}</h4>
			</div>
				<p><a href="{{ path('voirArticle', {id: article.id}) }}">Voir</a>
					
				</p>
			</article>
			{% endfor %}
			

		{% endblock %}
		